[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
timeout = 60
timeout_method = thread
markers =
    integration: tests that hit real network, subprocess, or live engine (slow, may hang without infra)
    slow: tests that take >5s but are hermetic
