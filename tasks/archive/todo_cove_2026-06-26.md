# Vector 3: Self-Reflection — Chain-of-Verification (CoVe)

## Goal
Add reasoning-quality validation to the existing Critic. The Critic currently
checks format + fact-contradiction only. CoVe forces the model to:
1. Extract every factual claim from the draft answer.
2. Cite supporting evidence from tool data (or NONE).
3. Audit logical derivation — flag leaps/assumptions/fallacies.

## Constraint (HARD — lessons.md:65-74)
KoboldCpp 4B cannot produce JSON. NEVER use `response_format`/`json_schema`.
Use structured plain-text parsed via regex.

## Changes (3 files, surgical)

### 1. `services/agent/_agent_critic.py` (127 → ~190 lines)
- Replace single-line PASS/FAIL prompt with CoVe 3-step prompt.
- Replace single-line parser with multi-line section parser
  (VERDICT / CLAIMS / EVIDENCE / LOGICAL_FLAW / REASON).
- Populate `extracted_claims`, `missing_facts`, `logical_flaw` in feedback.
- Contradiction heuristic v2: flip PASS→FAIL if any claim ungrounded OR flaw exists.
- Keep legacy Hebrew negative-phrase backstop.
- `max_tokens` 128 → 256 (structured output needs room).
- `_mk_critic_fb` gains optional `claims`/`missing_facts`/`logical_flaw` kwargs
  (backward-compatible defaults).

### 2. `services/agent/_nodes/_critic.py` (149 → ~165 lines)
- Populate `ctx._critic_claims_history` from `feedback["extracted_claims"]`
  (field exists at `_context.py:56`, currently never written).
- Surface `logical_flaw` + `missing_facts` explicitly in the feedback message
  injected into `ctx.messages` so the agent learns the specific logical error
  in the retry round.

### 3. `services/agent/_context.py` — NO CHANGE
- `_critic_claims_history` already declared (line 56).
- `logical_flaw` carried in feedback dict (untyped), no new field needed (YAGNI).

## Verification
- `.\.venv\Scripts\python.exe bin\lint-gate.py`
- `.\.venv\Scripts\python.exe -m pytest tests/agent/ -k critic -q` (if exists)
- Manual: trigger a draft with an unsupported logical leap → confirm FAIL +
  logical_flaw text appears in retry feedback.

## Review
(pending)
