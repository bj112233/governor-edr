<Sysmon schemaversion="4.91">
  <!-- Minimal config for spike: Process Create (Event 1) only.
       Hashing: SHA256 only (faster than all algorithms).
       onmatch="exclude" with empty filter = log everything (no exclusions). -->
  <HashAlgorithms>SHA256</HashAlgorithms>
  <EventFiltering>
    <ProcessCreate onmatch="exclude">
      <!-- No exclusions = log all process creations -->
    </ProcessCreate>
    <!-- Explicitly disable other event types to reduce noise -->
    <FileCreateTime onmatch="exclude" />
    <NetworkConnect onmatch="exclude" />
    <ProcessTerminate onmatch="exclude" />
    <ImageLoad onmatch="exclude" />
    <DriverLoad onmatch="exclude" />
    <FileCreate onmatch="exclude" />
    <RegistryEvent onmatch="exclude" />
    <FileCreateStreamHash onmatch="exclude" />
    <PipeEvent onmatch="exclude" />
    <WmiEvent onmatch="exclude" />
    <DnsQuery onmatch="exclude" />
    <FileDelete onmatch="exclude" />
    <ProcessAccess onmatch="exclude" />
    <FileDeleteDetected onmatch="exclude" />
  </EventFiltering>
</Sysmon>
